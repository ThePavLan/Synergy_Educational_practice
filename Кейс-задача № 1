# Кейс-задача № 1
# Дан одномерный массив А размерности N. Найти сумму отрицательных элементов, расположенных между максимальным и минимальным. 
# Ответом на задачу будет ссылка на репозиторий GitHub, где хранится Ваша программа. Или иным удобным для Вас способом.


# Можно воспользоваться следующим алгоритмом:
# Найти минимальный элемент массива и его индекс.
# Найти максимальный элемент массива и его индекс.
# Просуммировать все отрицательные элементы, находящиеся между этими двумя индексами.



def sum_between_min_max(arr):
    # Найдем минимальные и максимальные элементы и их индексы
    min_value = float('inf')
    max_value = float('-inf')
    min_index = -1
    max_index = -1
    
    for i in range(len(arr)):
        if arr[i] < min_value:
            min_value = arr[i]
            min_index = i
        if arr[i] > max_value:
            max_value = arr[i]
            max_index = i
            
    # Определим границы диапазона для суммирования
    start = min(min_index, max_index) + 1
    end = max(min_index, max_index)
    
    # Суммируем отрицательные элементы между границами
    total_sum = 0
    for i in range(start, end):
        if arr[i] < 0:
            total_sum += arr[i]
    
    return total_sum

# Пример использования функции
arr = [-5, 7, -8, 12, -13, 20, -25, 30]
result = sum_between_min_max(arr)
print(result)  # Ожидаемый результат: -46

# Пояснения к программе:
# Поиск минимума и максимума: Используем переменные min_value, max_value, min_index и max_index для отслеживания текущего минимума, максимума и их индексов. Начальное значение для минимума — бесконечность (float('inf')), а для максимума — минус бесконечность (float('-inf')).
# Определение границ диапазона: Определяем начальный и конечный индексы для суммирования, используя минимальное и максимальное значения индексов.
# Суммирование отрицательных элементов: Проходим по всем элементам между найденными границами и суммируем те, которые меньше нуля.
# Программа выводит итоговую сумму отрицательных элементов, находящихся между минимальным и максимальным элементами массива.
